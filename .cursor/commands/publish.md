# publish

Publish core package and sync dependencies to consuming projects.

**Note:** Ensure consuming projects have the latest code (`git pull`) before running this command.

```bash
pnpm publish && pnpm sync-deps:fix
```

This command will be available in chat with /publish
