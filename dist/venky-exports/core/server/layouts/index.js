'use strict';
/* Copyright (c) 2024-present Venky Corp. */
/**
 * Server-rendered route layouts that consuming projects re-export as their own
 * `layout.tsx` default. These are React Server Components that read config and may
 * redirect, so they live behind the `browser`-stubbed `venky-core/server/layouts`
 * entry. Add new layouts here — no package.json change needed per layout.
 */
//# sourceMappingURL=index.js.map
