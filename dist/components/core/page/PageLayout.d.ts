import { type ReactNode } from 'react';
export default function PageLayout({ icon, leftSection, mainSection, filterSection, subTitle, title, toolbar, children, transparentMainSection, enableShareUrl, showThemeToggle, showUserProfile, enableComments, statsSection, }: {
    icon?: ReactNode;
    leftSection?: ReactNode;
    mainSection?: ReactNode;
    filterSection?: ReactNode;
    subTitle?: ReactNode;
    title: ReactNode;
    toolbar?: ReactNode;
    children?: ReactNode;
    transparentMainSection?: boolean;
    enableShareUrl?: boolean;
    enableComments?: boolean;
    showThemeToggle?: boolean;
    showUserProfile?: boolean;
    statsSection?: ReactNode;
}): import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=PageLayout.d.ts.map